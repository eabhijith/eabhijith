/** readme/sections/activity.mjs — self-generated contribution activity graph */
import { USERNAME } from "../../brand.mjs";
const RAW = `https://raw.githubusercontent.com/${USERNAME}/${USERNAME}/main`;

export function render() {
  return `<p align="center">
  <img src="${RAW}/dist/activity.svg" width="100%" alt="Contribution activity over the last 12 months"/>
</p>
`;
}
